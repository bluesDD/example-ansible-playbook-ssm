---
- hosts: localhost
  tasks:
  - name: Enable to install NGINX.
    shell: "amazon-linux-extras enable nginx"
    become: yes

  - name: Install NGINX.
    yum: name=nginx

  - command: chkconfig nginx on

  - command: systemctl nginx restart
